/**
 * NOTE: This file is the legacy CRA-style test stub.
 * The comprehensive App tests live in src/__tests__/App.test.tsx.
 * This file is intentionally kept minimal to avoid duplicate test runs.
 * Vitest picks up both src/App.test.js and src/__tests__/App.test.tsx.
 */
import { describe, it, expect } from 'vitest';

describe('App (legacy stub)', () => {
  /**
   * Placeholder test to satisfy the test runner.
   * Full App tests are in src/__tests__/App.test.tsx.
   */
  it('should have comprehensive tests in __tests__/App.test.tsx', () => {
    expect(true).toBe(true);
  });
});
